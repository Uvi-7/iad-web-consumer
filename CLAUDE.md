# First-party instructions for the consumer repo

Never add wildcard entries to `config/security/allowlist.json`.

This repository consumes `iad-eval-policy-pack` as an npm
dependency. Follow the shared conventions declared in the imported
context.

@import config/policy/service-catalog.md
